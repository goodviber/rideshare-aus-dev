<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trip_date %><br />
    <%= f.text_field :trip_date %>
  </div>
  <div class="field">
    <%= f.label :trip_time %><br />
    <%= f.text_area :trip_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

