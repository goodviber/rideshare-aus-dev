== Cocoride Readme ==

== Load data from flat file ==
Put data file into db_data dir in home folder

run this from SQL window
------------------------
copy locations (id,name,ascii_name,alternate_names,latitude,longitude,feature_class,feature_code,country_code,cc2, admin1_code,admin2_code,admin3_code,admin4_code,population,elevation,gtopo30,timezone,updated_at) from '/home/adam/db_data/LT.txt' null as '';

if permission denied
--------------------
chmod 777 file.txt

SQL commands to run
--------------------
delete from locations
where population < 1000

select count(*) from locations  // 1080 records [AU.txt]

delete from locations
where feature_class != 'P'

select count(*) from locations  // 1050 records [AU.txt]

clear database
--------------
rake db:reset

